# Discord Internship Search Webhook

A python script for searching internships using DGBG search library. Once it finds the internship, sends the results via Discord Webhook.

The Github Action executes the script for every 1 hour.
